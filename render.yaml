services:
  - type: web
    name: smart-healthcare-analytics
    runtime: python
    plan: free
    healthCheckPath: /api/health
    buildCommand: pip install -r backend/requirements.txt
    startCommand: python run_project.py
    envVars:
      - key: DATA_DIR
        value: /tmp/smart-healthcare-analytics
